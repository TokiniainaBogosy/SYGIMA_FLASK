export default function StatCard(props) {
  // 1. Récupère les props
  // const { label, value, icon, color } = props
  
  
  // 2. Retourne le JSX
  return (
    <div className="...">
      
    </div>
  )
}